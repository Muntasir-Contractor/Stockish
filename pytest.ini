[pytest]
# "." puts the repo root on sys.path (for `backend.main`, `scripts.valuation_models`);
# "backend" satisfies main.py's bare imports (`fetchfromAPI`, `application`, `db_funcs`).
pythonpath = . backend
testpaths = backend/tests
